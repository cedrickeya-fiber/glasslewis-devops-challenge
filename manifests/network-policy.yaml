apiVersion: networking.k8s.io/v1
kind: NetworkPolicy

metadata:
  name: store-network-policy
  namespace: store-app

spec:
  podSelector: {}

  policyTypes:
  - Ingress
  - Egress

  ingress:
  - from:
    - podSelector: {}

  egress:
  - to:
    - podSelector: {}